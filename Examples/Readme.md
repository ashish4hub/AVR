# Example
## led_blink_delay
* This program demonstrate the GPIO control on avr microcontroller using direct register access (bare metal programming). An led connected to **PD2** is toggled on and off with a 1 second delay.
